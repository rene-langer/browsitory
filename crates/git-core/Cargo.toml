[package]
name = "git-core"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
git2 = { version = "0.21", features = ["https", "ssh"] }
thiserror = "1.0"
url = "2.5"

[dev-dependencies]
tempfile = "3"
