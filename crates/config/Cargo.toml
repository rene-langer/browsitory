[package]
name = "config"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
directories = "6"
serde = { version = "1", features = ["derive"] }
toml = "1"
thiserror = "1.0"

[dev-dependencies]
tempfile = "3"
