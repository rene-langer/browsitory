# Browsitory for VSCode

Browsitory is a desktop-only Git repository browser for Visual Studio Code.

Install a platform-matching package with:

```sh
code --install-extension browsitory-<version>-<platform>.vsix
```

Then run **Browsitory: Open** from the Command Palette. The VSIX includes the matching native
sidecar; it does not support vscode.dev or Codespaces.
